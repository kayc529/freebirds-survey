<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <p>Survey List</p>
    <!-- Show a list of surveys here -->
    <% for(let count=0; count < surveys.length; count++){ %>
        <a href="/survey/do-survey/<%= surveys[count]._id %>"><%= surveys[count].title %></a>
        <a href="/survey/survey-results/<%= surveys[count]._id %>">Results</a>
    <% } %>
</body>
</html>